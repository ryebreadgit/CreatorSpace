version: '3'
services:
  app:
    build:
      context: https://github.com/ryebreadgit/CreatorSpace.git
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - /CreatorSpace/downloads:/path/to/downloads
      - /CreatorSpace/settings.json:/path/to/appdata/CreatorSpace/settings.json
      - /CreatorSpace/database.db:/path/to/appdata/CreatorSpace/database.db # optional for sqlite
